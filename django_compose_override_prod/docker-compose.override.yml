version: "2.2"

services:
    db:
        image: postgres:15-alpine
        volumes:
            - ./data/pgdata-15:/pgdata
            - ./data/logs/db:/pg_log
            - ./db_backups:/db_backups
