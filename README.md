# Minimal Django Shop Core Project

1. clone smartshop_template `git clone git@gitlab.smartbase.sk:dev/smartshop_template.git`
2. remove "project" folder
3. copy and override all files and folders from django-minimal-project to the cloned smarthsop_template project

© SmartBase
